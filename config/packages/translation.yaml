framework:
    default_locale: fr
    enabled_locales: '%app.locales%'
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - fr
        providers:
