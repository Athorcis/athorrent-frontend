{% extends 'pages/fileDetails.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('displayFile') }}
{% endblock %}

{% block main_script %}{% endblock %}

{% block content %}
    {{ parent() }}

    {% if text is defined %}
        <pre>{{ text }}</pre>
    {% elseif src is defined %}
        <img class="content" src="{{ path('downloadFile', { path: src }) }}" />
    {% endif %}
{% endblock %}
